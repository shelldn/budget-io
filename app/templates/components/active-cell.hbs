{{#if isEditing}}
  {{active-cell-input disabled value=value on-change=(action "commit") on-complete=(action "hideEditor")}}
{{else}}
  <div {{action "showEditor" on="doubleClick"}}>
    {{value}}
  </div>
{{/if}}
